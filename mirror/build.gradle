plugins {
    id 'java-library'
    id 'maven-publish'
    id 'checkstyle'
    id 'jacoco'
    alias libs.plugins.yumi.gradle.licenser.plugin
    alias libs.plugins.jaredsburrows.license.plugin
}

java {
    withJavadocJar()
    withSourcesJar()
}

dependencies {
    compileOnly libs.slf4j.api

    implementation libs.classgraph
    compileOnly libs.jakarta.validation.api
    implementation libs.jackson2.core
    implementation libs.jackson2.databind
    implementation libs.jackson2.datatype.jdk8
    implementation libs.jackson2.datatype.jsr310
    implementation libs.jackson2.module.parameter.names

    implementation (libs.type.resolver) {
        exclude (group: 'org.apache.logging.log4j', module: 'log4j-core')
    }

    api project(":types")
    implementation project(":reflect")

    testImplementation libs.logback.classic
    testImplementation project(":test-shared-impl")
    testImplementation project(":builder")
    testImplementation project(":type-utils")
    testImplementation libs.junit.jupiter.params
    testImplementation libs.lombok
    testImplementation libs.assertj
    testAnnotationProcessor libs.lombok
}

test {
    finalizedBy jacocoTestReport
}
jacocoTestReport {
    dependsOn test
}

jacocoTestCoverageVerification.dependsOn jacocoTestReport
